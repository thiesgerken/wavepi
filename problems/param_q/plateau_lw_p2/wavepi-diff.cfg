# WavePI settings file
# generated by wavepi c088bec9-dirty (built 2018-03-21T12:44:28Z)
subsection inversion
  set method = NonlinearLandweber # default: REGINN
  subsection NonlinearLandweber
    set omega = 1e3 # default: 1
  end
end
subsection mesh
  subsection shape
    set options        = left=-4.0, right=4.0 # default: left=-1.0, right=1.0
  end
end
subsection output
  set interval             = 0             # default: 10
end
subsection problem
  set initial guess    = 0.0                                           # default: 0.5
  set norm of domain   = H1L2                                          # default: L2L2
  set parameter q      = 2*if(norm{x|y|z} < 2.0, 2.0+abs(sin(t)), 0.0) # default: 0.0
  set type             = q                                             # default: a
  subsection data
    set right hand sides           = if(t<3, if(norm{x|y|z} < 0.5, sin(3*t), 0.0), 0) # default: if(norm{x|y|z} < 0.2, sin(t), 0.0)
  end
end

# WavePI settings file
# generated by wavepi f124684b-dirty (built 2018-03-01T13:22:47Z)
subsection inversion
  subsection REGINN
    set linear solver            = GradientDescent

    subsection GradientDescent
      set p = 2
    end
  end
end

subsection mesh
  set initial refines    = 5
  subsection shape
    set options        = left=-4.0, right=4.0 # default: left=-1.0, right=1.0
  end
end
subsection output
  set discrepancy decrease = 0.25
  set interval             = 0            # default: 10
end
subsection problem
  set epsilon          = 1e-2     # default: 1e-2
  set initial guess    = 0.0      # default: 0.5
  set norm of domain   = Coefficients     # default: L2L2
  set parameter q      = 2*if(norm{x|y|z} < 2.0, 2.0+abs(sin(t)), 0.0) # default: 2.0
  set type             = q        # default: a
  subsection data
    set configurations             = 0                                                                                                                                                                                                                                 # default: 0
    set number of right hand sides = 1                                                                                                                                                                                                                                       # default: 1
    set right hand sides           = if(t<3, if(norm{x|y|z} < 0.5, sin(3*t), 0.0), 0)
    subsection config 0
      set measure             = Field
    end
  end
end

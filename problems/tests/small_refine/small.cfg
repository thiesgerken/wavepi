# WavePI settings file
# generated by wavepi f124684b-dirty (built 2018-03-01T13:22:47Z)
subsection LogTransform
  set lower bound = 0.1 # default: 0.0
end
subsection inversion
  subsection REGINN
    set maximum iteration choice = Fibonacci          # default: Constant
  end
end
subsection log
  # depth for stdout
  set console depth = 2

  # external log file
  set file          = wavepi.log

  # depth for the log file
  set file depth    = 100
end
subsection mesh
  set initial time steps = 128 # default: 256
  set initial refines = 5
  subsection shape
    set options        = left=-4.0, right=4.0 # default: left=-1.0, right=1.0
  end
end
subsection output
  set discrepancy decrease = 0.5           # default: 0.25
  set interval             = 50            # default: 10
end
subsection problem
  set epsilon          = 1e-2 # default: 1e-2
  set initial guess    = 1.0  # default: 0.5
  set norm of domain   = H1L2 # default: L2L2
  set parameter c      = 1.5  # default: 2.0
  set transform        = Log  # default: Identity
  set type             = c    # default: a
  subsection data
    set right hand sides           = if(t<3, if(norm{x|y|z} < 0.2, sin(3*t), 0.0), 0) # default: if(norm{x|y|z} < 0.2, sin(t), 0.0)
    set additional fe degrees      = 0
    set additional refines         = 1
  end
end


# WavePI settings file
# generated by wavepi f124684b-dirty (built 2018-03-01T13:22:47Z)
subsection constraints
  set lower bound = 0.1   # default: -1e300
end
subsection inversion
  subsection REGINN
    set maximum iteration choice = Fibonacci          # default: Constant
  end
end
subsection mesh
  set initial refines    = 6   # default: 5
  subsection shape
    set options        = left=-4.0, right=4.0 # default: left=-1.0, right=1.0
  end
end
subsection output
  set discrepancy decrease = 0.5           # default: 0.25
  set interval             = 50            # default: 10
end
subsection problem
  set initial guess    = 1.0      # default: 0.5
  set parameter c      = 1.5      # default: 2.0
  set type             = c        # default: a
  subsection data
    set configurations             = 0;0                                                                                                                         # default: 0
    set number of right hand sides = 2                                                                                                                           # default: 1
    set right hand sides           = if(t<3, if(norm{x-1.3|y-1.3|z-1.3} < 0.25, sin(3*t), 0.0), 0);if(t<3, if(norm{x+1.3|y+1.3|z+1.3} < 0.25, sin(3*t), 0.0), 0) # default: if(norm{x|y|z} < 0.2, sin(t), 0.0)
  end
end
